#include <stdio.h>
  int soma(int a, int b) {
      int c;
      c=a+b;
      return c;
  }
  
int main()
{
   int a, b;
   
   printf("\nInforme dois numeros\n");
   scanf("%d%d", &a, &b);
   printf("a soma dos dois numeros é %d", soma(a,b));
   return 0;
  
}
