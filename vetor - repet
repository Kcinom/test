#include <stdio.h>
int number[10];
int main ()
{
  printf ("\t\t\tEnter the numbers to fill in:\n");
  for (int i = 0; i <= 9; i++)
	{
	  scanf ("%d", &number[i]);
	}
	 printf ("\n\n");
	for (int i = 0; i <= 9; i++)
	{
	  printf ("%d\n", number[i]);
	}
  return 0;
}
