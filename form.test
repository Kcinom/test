#include <stdio.h>
    float a, b, c, d, e, f, x, y;
int main()
{
    printf ("\t\tInform a, b, c, d, e and f\n");
    scanf ("%f%f%f%f%f%f", &a, &b, &c, &d, &e, &f);
    x = (((c * e) - (b * f))/((a * e) - (b * d)));
    y = (((a * f) - (c * d))/((a * e) - (b * d)));
    printf ("\n\t\tYour 'x' is %.2f and your 'y' is %.2f. \n", x, y);
    return 0;
}
